# Unknown + Coverage

Unknown:
```ts
{ status: 'unknown' }
```

Không:
```ts
{ estimatedDepthCm: 0 }
```

Coverage theo chiều dài:
`knownLength / totalLength`

UI luôn hiển thị:
- Độ phủ dữ liệu: XX%
- N đoạn chưa đủ dữ liệu

Unknown map style:
- neutral
- dashed
- tooltip `Chưa đủ dữ liệu`
