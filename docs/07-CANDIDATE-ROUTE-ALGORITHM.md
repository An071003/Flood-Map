# Candidate Route Algorithm

Possible: k-shortest paths, Yen, weighted A*, or external route alternatives then flood re-score.
Reject alternatives with excessive overlap. Return 1–3 meaningful candidates. Never duplicate one route under three labels.
