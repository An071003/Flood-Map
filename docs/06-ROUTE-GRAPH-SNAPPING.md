# Route Graph Snapping

Search coverage != routing coverage.

Snap result contains input point, snapped point, segmentId, distanceMeters, exact flag.

If snap distance is material, tell user explicitly. Never silently snap far away.
