# Route Cost Engine

segmentCost = timeCost + depthPenalty + riskPenalty + unknownPenalty + lowConfidencePenalty.

Retain max depth, max risk, severe count, warning count, unknown count, coverage, travel time.

Least Flood: flood weight high. Balanced: mixed. Fastest: time high but still flood-block/penalize.
Every candidate needs explanation.
