# Candidate Diversity

Meaningful alternatives only.

Use shared segment length:
```ts
overlapRatio =
  sharedLength /
  Math.min(routeALength, routeBLength)
```

If too similar:
- remove duplicate
- explain reduced candidate count

Do not force exactly 3 cards.
